# Python venv
.venv/

# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/

# Claude Code: commit the shared permission policy (settings.json),
# but keep personal/local overrides out of the repo
.claude/settings.local.json

# OS / editor noise
.DS_Store
.vscode/
.idea/
